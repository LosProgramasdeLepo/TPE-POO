package backend.model;

import javafx.scene.canvas.GraphicsContext;

public interface Drawable {

    //draws an element (figure) on a GraphicsContext
    void draw(GraphicsContext graphicsContext);

}
